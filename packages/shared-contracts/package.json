{
  "name": "@smartprinter/shared-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types for IPC and domain models, mirroring the C# agent's contracts.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
