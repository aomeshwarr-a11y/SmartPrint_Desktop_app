# Copy this file to .env (for apps/desktop-ui, Vite reads .env files from that package's
# directory - copy it to apps/desktop-ui/.env, or use `cp .env.example apps/desktop-ui/.env`)
# and fill in real values. Never commit the filled-in .env file.

# ---------------------------------------------------------------------------
# Supabase (used by BOTH the Electron UI renderer and, separately, by the C# agent's
# own appsettings.json / environment variables - see services/desktop-agent/SmartPrinter.Agent/appsettings.json)
# ---------------------------------------------------------------------------
VITE_SUPABASE_URL=https://geydfdzltlofdwcvorhn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdleWRmZHpsdGxvZmR3Y3ZvcmhuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODg5NTg3NjAsImV4cCI6MjEwNDUzNDc2MH0.17i0rxludyk8Buexa5H31tT8bnuVjuWWsLWfoo0JkvA

# ---------------------------------------------------------------------------
# C# Agent configuration overrides (environment variable form of appsettings.json -
# see services/desktop-agent/SmartPrinter.Agent/Configuration/*.cs for the full set).
# These use the SMARTPRINTER_ prefix and double-underscore section separator that
# Microsoft.Extensions.Configuration's environment variable provider expects.
# ---------------------------------------------------------------------------
SMARTPRINTER_Supabase__Url=https://geydfdzltlofdwcvorhn.supabase.co
SMARTPRINTER_Supabase__AnonKey=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdleWRmZHpsdGxvZmR3Y3ZvcmhuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODg5NTg3NjAsImV4cCI6MjEwNDUzNDc2MH0.17i0rxludyk8Buexa5H31tT8bnuVjuWWsLWfoo0JkvA
SMARTPRINTER_Agent__MockCloudMode=false


# ---------------------------------------------------------------------------
# Supabase Edge Functions secrets (set these with `supabase secrets set KEY=value`,
# NEVER put them in this file or any .env committed to the repo - they belong only in
# the Supabase project's function secrets store).
# ---------------------------------------------------------------------------
# SUPABASE_JWT_SECRET=            (from Project Settings > API > JWT Settings)
# RAZORPAY_WEBHOOK_SECRET=        (from the Razorpay dashboard webhook configuration)
RAZORPAY_KEY_ID= rzp_live_THMeJ8Iy04bPIZ
RAZORPAY_KEY_SECRET=a4QWFZkq1Pn8AwGCBAwHfbEp

# ---------------------------------------------------------------------------
# electron-builder code signing (set as real environment variables at package time,
# never committed)
# ---------------------------------------------------------------------------
# CSC_LINK=path-or-url-to-your-ev-cert.pfx
# CSC_KEY_PASSWORD=your-cert-password
